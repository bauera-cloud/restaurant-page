inspiration: https://ginnerzapata.github.io/restaurant/

project goal: practice ES6 Modules and Webpack

plan: 

//index.js
import { navbar, footer } from './partials.js'
import homepageContent from './homepage.js'
import menuContent from './menu.js'
import aboutContent from './about.js'

load homepage
    content.appendChild(navbar)
    content.appendChild(homepageContent)
    content.appendChild(footer)

1st option:
add and remove div from between the navbar div and footer div

if user clicks menu 
    remove mainContentDiv
    add menuContent
if user clicks about
    remove mainContentDiv
    add aboutContent
if user clicks home
    remove mainContentDiv
    add homepageContent

??2nd option
mainContentDiv = homepageContent
if user clicks menu
    mainContentDiv = menuContent
if user clicks about
    mainContentDiv = aboutContent
if user clicks home
    mainContentDiv = homepageContent

3rd option: 
homepage.js etc. uses the partials, add div to content
if user clicks menu link in the nav bar, delete div from content, add menu div to content

if user clicks menu
    content.removeChild()
    content.appendChild(menuContent) //menuContent is the div (that includes partials) from menu.js

I like the 3rd option the most

//POSSIBLE SOLUTION: https://webpack.js.org/guides/output-management/
//importing div from another file
